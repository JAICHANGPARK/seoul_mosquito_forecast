name: flutter_seoul_mosquito_forcast
description: A new Flutter application.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
authors:
  - JAICHANG.PARK(Dreamwalker)

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  http: any
  intl: ^0.16.1
  webview_flutter: ^0.3.22+1
  share: ^0.6.4+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/img/
